/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Cambiar x dominio de donde saque las pics
    domains: ["images.unsplash.com", "lh3.googleusercontent.com", "assets.aceternity.com"],
  },
};

export default nextConfig;
